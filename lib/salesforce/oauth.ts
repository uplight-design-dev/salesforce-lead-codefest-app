/** httpOnly cookie name used for OAuth CSRF state verification. */
export const OAUTH_STATE_COOKIE = "sf_oauth_state";

/** httpOnly cookie storing the PKCE code verifier for token exchange. */
export const OAUTH_PKCE_COOKIE = "sf_oauth_pkce";
